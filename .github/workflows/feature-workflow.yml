name: Feature Branch CI/CD Workflow

on:
  push:
    branches:
      - 'SEP490-*'
    paths:
      - 'src/**'
      - 'public/**'
      - 'package.json'
      - 'package-lock.json'
      - 'vite.config.ts'
      - 'index.html'
      - 'tsconfig.json'
      - 'tsconfig.app.json'
      - 'tsconfig.node.json'
      - 'resources/scripts/check-health.sh'
      - '.github/workflows/feature-workflow.yml'
      - '.github/workflows/deploy-workflow.yml'
      - '.github/actions/**'

jobs:
  code_quality:
    name: Verify Code Quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Project
        uses: ./.github/actions/setup-project

      - name: Static Check
        run: |
          npm run format:check
          npm run lint

  deploy_dev:
    name: Deploy to Dev
    needs: code_quality
    uses: ./.github/workflows/deploy-workflow.yml
    with:
      environment: dev
    secrets: inherit
